#pragma once
// Per-platform clip rect header. ClipRect is an AmLang struct so its
// C representation comes out of amlc itself — this header just gates
// the include order. Mirrors the empty amigaos / libc peers.
